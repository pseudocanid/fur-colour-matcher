Planned features:
- Multiple companies, with filters to select which companies are shown (ex. show BigZ, hide Miemiefur)
-   - If multiple companies are added, increase number of runner up matches
- Show (and possibly filter by) fur lengths (will use the same preview photo for multiple lengths; ex. if colour is in shag and beaver, only show the shag image since it gives a good enough idea)

Known Bugs:
- Entering fullscreen mode moves the magnifier to the right side of the page, and renders it below the right column
- Mobile support is low, even worse on IFRAME embed
  
Planned Tools:
- Merge, or allow txt import to my Fursona Recolour project. Choose which colours on the ref sheet will be recoloured by the fur colours, then perform automatically (and change colour distance algorithm to LAB).

Added May 2026
- Preview photo with fur swatch from listing by splitting the 'best match' and 'other matches' field in half
- Change export to export a collage img of the matching furs
